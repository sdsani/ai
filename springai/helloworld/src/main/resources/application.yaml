spring:
  application:
    name: helloworld
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          temperature: 0.7
          #model: gpt-5
          model: gpt-4.1-nano
